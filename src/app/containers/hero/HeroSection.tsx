import React from "react";

const HeroSection = () => {
  return (
    <main className="w-full bg-blue-500 z-10 h-screen flex items-center justify-center">
      {/* The w-full and h-screen classes make the div cover the full screen */}
      HeroSection
    </main>
  );
};

export default HeroSection;
