import React from "react";

const ResumeSection = () => {
  return (
    <main className="w-full bg-violet-500 z-10 h-screen flex items-center justify-center">
      {/* The w-full and h-screen classes make the div cover the full screen */}
      ResumeSection
    </main>
  );
};

export default ResumeSection;
