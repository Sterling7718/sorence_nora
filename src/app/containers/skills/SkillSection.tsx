import React from "react";

const SkillSection = () => {
  return (
    <main className="border w-full h-screen flex items-center justify-center">
      {/* The w-full and h-screen classes make the div cover the full screen */}
      SkillSection
    </main>
  );
};

export default SkillSection;
