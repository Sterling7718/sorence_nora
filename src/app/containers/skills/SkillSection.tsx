import React from "react";

const SkillSection = () => {
  return (
    <main className="w-full bg-orange-500 z-10 h-screen flex items-center justify-center">
      {/* The w-full and h-screen classes make the div cover the full screen */}
      SkillSection
    </main>
  );
};

export default SkillSection;
